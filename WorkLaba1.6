#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

#define BUFSIZ 1000  // Размер массива (можно менять)

int main() {
    srand(time(NULL));  // Инициализация генератора случайных чисел текущим временем

    // Массив для хранения случайных чисел
    int arr[BUFSIZ];

    // Генерируем случайные числа в диапазоне [-10000, 10000]
    for (int i = 0; i < BUFSIZ; i++) {
        arr[i] = rand() % 20001 - 10000;  // Генератор случайных чисел от -10000 до 10000
    }

    // Объявление начальных условий для поиска мин/макс
    int min_value = INT_MAX;       // Минимальное значение изначально максимально возможное
    int max_value = INT_MIN;       // Максимальное значение изначально минимально возможное
    int min_idx = -1;              // Индекс минимального элемента
    int max_idx = -1;              // Индекс максимального элемента

    // Однопроходный поиск мин/макс и их индексов
    for (int i = 0; i < BUFSIZ; i++) {
        if (arr[i] < min_value) {  // Нашли новый минимум
            min_value = arr[i];    // Сохраняем новое минимальное значение
            min_idx = i;           // Сохраняем индекс нового минимума
        }
        if (arr[i] > max_value) {  // Нашли новый максимум
            max_value = arr[i];    // Сохраняем новое максимальное значение
            max_idx = i;           // Сохраняем индекс нового максимума
        }
    }

    // Вывод результатов
    printf("Минимальный элемент: %d, индекс: %d\\", min_value, min_idx);
    printf("Максимальный элемент: %d, индекс: %d\\", max_value, max_idx);

    return 0;
}
